services:
  export:
    build: .
    user: root
    volumes:
      - .:/workspace
    working_dir: /workspace
    entrypoint: ["/bin/bash", "/action/kiforge.sh"]
    command: [
      "--project-path", "tests/sample_project",
      "--output-dir", "kiforge"
    ]
